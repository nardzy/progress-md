{
  "name": "client",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cross-env NODE_ENV=dev rspack dev",
    "build": "cross-env NODE_ENV=prod rspack build",
    "preview": "rspack preview"
  },
  "devDependencies": {
    "@rspack/cli": "^1.3.15",
    "@rspack/core": "^1.3.15",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
